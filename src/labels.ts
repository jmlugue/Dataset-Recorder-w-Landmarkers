// The seven approved Makaton gestures for the MakaLearn dataset (thesis Phase 1).
// Short lowercase forms double as the folder / filename-prefix labels used by the
// batch extractor, so keep them in sync with the recorded clip filenames.
export const gestureLabels = ["toilet", "eat", "drink", "help", "yes", "no", "sit"];
